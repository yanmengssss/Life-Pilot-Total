# 第4章 概要设计

第3章完成可行性分析后，系统进入概要设计阶段。本章直接对应当前项目的工程结构，围绕 `LifePilot` 前端、`LifePilotServer` 业务服务、`LifePilot_mcp` 工具服务与 `ai-server` 知识服务展开。章节目标是给出模块边界、协同路径、数据落点与接口组织，为第5章详细设计和第6章实现与测试提供统一框架。

## 4.1 设计目标

本项目的设计目标来自三类高频业务：任务规划与执行、知识入库与问答、个人状态记录。系统需要在同一应用内支持三类业务连续完成，减少页面跳转和信息割裂。系统还要支持持续迭代，前端、业务服务、工具服务和知识服务采用独立演进方式，便于后续扩展能力。

如图4-1所示，设计目标与系统能力形成固定映射关系。后续架构划分与流程组织均基于该映射执行。

```mermaid
flowchart TB
    G1[统一交互入口] --> C1[文本输入]
    G1 --> C2[语音输入]

    G2[任务闭环] --> C3[任务生成]
    G2 --> C4[状态跟踪]
    G2 --> C5[到期提醒]

    G3[知识闭环] --> C6[文档入库]
    G3 --> C7[检索问答]

    G4[工程可演进] --> C8[多服务解耦]
    G4 --> C9[容器化部署]
```

图4-1 设计目标与能力映射

图4-2给出本章覆盖范围。概要设计关注系统结构和协同逻辑，不进入代码实现细节。

```mermaid
flowchart LR
    A[第3章 可行性分析] --> B[第4章 概要设计]
    B --> C[第5章 详细设计]
    C --> D[第6章 实现与测试]
    B1[模块边界] --> B
    B2[业务流程] --> B
    B3[数据与接口] --> B
```

图4-2 章节衔接关系

## 4.2 系统总体架构

系统采用四层结构。表现层对应 `LifePilot`，负责页面组织与交互承载。业务层对应 `LifePilotServer`，负责对话编排、工作流路由和调度控制。工具层对应 `LifePilot_mcp`，负责任务域数据操作。能力层对应 `ai-server`，负责文档解析、检索问答与语音处理。数据层由 MySQL、MongoDB、Redis、向量存储和对象存储组成。

如图4-3所示，当前版本的部署节点与连接方向已经固定，主链路从前端进入业务服务，再按意图分发至工具服务和知识服务。

```mermaid
flowchart TB
    U[用户浏览器] --> FE[LifePilot 前端]
    FE --> BE[LifePilotServer 5000]
    BE --> MCP[LifePilot_mcp 7000]
    BE --> RAG[ai-server 7100]

    MCP --> MYSQL[(MySQL 任务与用户数据)]
    BE --> MONGO[(MongoDB 对话记录)]
    BE --> REDIS[(Redis 调度队列)]

    RAG --> WEAV[(Weaviate 向量检索)]
    RAG --> QDR[(Qdrant 检索索引)]
    RAG --> NEO[(Neo4j 图结构)]
    RAG --> OSS[(七牛对象存储)]

    FE --> LOG[Metaphorical 日志服务]
    LOG --> LOGDB[(MongoDB 日志库)]
```

图4-3 系统总体架构图

图4-4展示一次完整请求的协同过程。该时序覆盖当前项目中的核心调用关系。

```mermaid
sequenceDiagram
    actor User as 用户
    participant FE as LifePilot
    participant BE as LifePilotServer
    participant RT as 工作流路由
    participant MCP as LifePilot_mcp
    participant RAG as ai-server
    participant DB as 多源存储

    User->>FE: 输入请求
    FE->>BE: 提交请求
    BE->>RT: 意图识别
    RT->>MCP: 任务域操作
    RT->>RAG: 知识域操作
    MCP->>DB: 写入或查询任务数据
    RAG->>DB: 查询知识索引与文档数据
    DB-->>BE: 返回结果
    BE-->>FE: 流式返回
    FE-->>User: 展示反馈
```

图4-4 业务协同时序图

## 4.3 功能模块划分

前端模块按用户任务组织，主页面覆盖清单、日历、知识库与记录四类入口。业务服务模块按能力组织，包含对话编排、任务规划、任务操作、出行规划、检索问答和提醒调度。工具服务负责任务和标签相关操作。知识服务负责文档解析、检索召回、语音识别和语音合成。

如图4-5所示，前端功能围绕 `home` 主路径组织，知识库和记录功能在同一应用内独立成页，和任务模块保持联动。

```mermaid
flowchart TB
    HOME[home 主区域]
    HOME --> LIST[任务清单页面]
    HOME --> CAL[日历页面]
    HOME --> KNOW[知识库页面]
    HOME --> REC[个人记录页面]

    LIST --> CHAT[对话入口]
    CAL --> CHAT
    KNOW --> CHAT
    REC --> CHAT
```

图4-5 前端功能结构图

如图4-6所示，业务服务内部以路由节点组织多个工作流。不同工作流共享上下文准备节点，再进入各自处理链路。

```mermaid
flowchart LR
    IN[对话请求] --> ROUTER[路由节点]
    ROUTER --> W1[任务规划流程]
    ROUTER --> W2[任务操作流程]
    ROUTER --> W3[出行规划流程]
    ROUTER --> W4[路线查询流程]
    ROUTER --> W5[知识问答流程]

    W1 --> MCP[工具服务]
    W2 --> MCP
    W5 --> RAG[知识服务]
```

图4-6 业务服务工作流结构图

## 4.4 核心设计视图

第3章已经给出业务执行流程，本节改为展示系统设计视图。图4-7至图4-11分别覆盖职责分配、状态模型、事件协议、索引结构与调度协作。该组织方式与第3章形成分工：第3章偏过程验证，第4章偏结构设计。

如图4-7所示，系统以“前端交互层、业务编排层、工具服务层、知识服务层”分配职责，各层通过明确接口协作。

```mermaid
flowchart LR
    FE[前端交互层]
    BE[业务编排层]
    MCP[工具服务层]
    RAG[知识服务层]
    OBS[监控与日志层]

    FE --> BE
    BE --> MCP
    BE --> RAG
    FE --> OBS
    BE --> OBS
    MCP --> OBS
    RAG --> OBS
```

图4-7 分层职责视图

如图4-8所示，任务对象以状态迁移组织，提醒逻辑和超时逻辑以状态变化为触发条件。

```mermaid
stateDiagram-v2
    [*] --> 待执行
    待执行 --> 进行中: 启动执行
    进行中 --> 已完成: 正常完成
    进行中 --> 已超时: 超过截止时间
    待执行 --> 已删除: 用户删除
    进行中 --> 已删除: 用户删除
    已完成 --> [*]
    已超时 --> [*]
    已删除 --> [*]
```

图4-8 任务状态模型图

如图4-9所示，对话链路采用事件协议返回内容。事件类型用于区分过程信息、结果信息和界面刷新信号。

```mermaid
sequenceDiagram
    participant FE as 前端
    participant BE as 业务服务
    participant WF as 工作流引擎

    FE->>BE: 提交对话请求
    BE->>FE: 事件 标识信息
    BE->>WF: 启动工作流
    WF-->>BE: 事件 过程信息
    BE-->>FE: 事件 过程信息
    WF-->>BE: 事件 回复信息
    BE-->>FE: 事件 回复信息
    WF-->>BE: 事件 刷新信号
    BE-->>FE: 事件 刷新信号
    BE-->>FE: 事件 完成信号
```

图4-9 对话事件协议图

如图4-10所示，知识服务将原始文档拆分为片段，再分别写入向量索引和图结构索引，问答阶段从双索引取回证据后生成结果。

```mermaid
flowchart TB
    D[原始文档] --> C[文本切分]
    C --> V[向量索引]
    C --> G[图结构索引]
    V --> R[证据召回]
    G --> R
    R --> A[答案生成]
```

图4-10 知识索引结构图

如图4-11所示，调度模块与缓存、任务库和通知模块形成协作闭环。到期任务先从队列取出，再根据任务状态执行提醒或结束处理。

```mermaid
sequenceDiagram
    participant SCH as 调度模块
    participant REDIS as 时间队列
    participant MYSQL as 任务库
    participant MAIL as 通知模块

    SCH->>REDIS: 拉取到期任务
    SCH->>REDIS: 抢占任务
    SCH->>MYSQL: 读取任务与用户配置
    alt 任务到期
        SCH->>MYSQL: 写入超时状态
        SCH->>MAIL: 发送超时通知
    else 任务未到期
        SCH->>MAIL: 发送提醒通知
        SCH->>REDIS: 写回下次触发时间
    end
```

图4-11 调度协作视图

## 4.5 数据与接口概要

数据层采用职责分工方式。MySQL承载任务、用户、标签与会话索引数据。MongoDB承载对话消息与日志记录。Redis承载调度时间队列。知识服务将文档切片写入向量存储，并维护图结构索引。媒体文件统一进入对象存储。

如图4-12所示，当前项目的数据落点关系已经固定，业务域与存储域呈一对多映射。

```mermaid
flowchart TB
    D1[任务域] --> S1[(MySQL)]
    D2[用户域] --> S1
    D3[会话索引] --> S1
    D4[对话消息] --> S2[(MongoDB)]
    D5[系统日志] --> S2
    D6[调度队列] --> S3[(Redis)]
    D7[文档向量] --> S4[(Weaviate/Qdrant)]
    D8[知识图结构] --> S5[(Neo4j)]
    D9[视频与文件] --> S6[(对象存储)]
```

图4-12 数据存储映射图

接口层按通信对象分为三组：前端到业务服务、业务服务到工具服务、业务服务到知识服务。前端对话链路使用流式返回，文件和任务操作使用请求响应。服务间调用采用协议化或标准 HTTP 调用，接口语义围绕任务、知识和媒体三类业务组织。

如图4-13所示，不同接口通道承担不同职责，接口层和存储层之间没有跨域直连。

```mermaid
flowchart LR
    FE[LifePilot 前端]
    BE[LifePilotServer]
    MCP[LifePilot_mcp]
    RAG[ai-server]
    DB1[(任务数据)]
    DB2[(知识数据)]

    FE -- 对话流接口 --> BE
    FE -- 任务与文件接口 --> BE
    BE -- 工具协议调用 --> MCP
    BE -- 检索与媒体调用 --> RAG
    MCP --> DB1
    RAG --> DB2
```

图4-13 接口分层关系图
