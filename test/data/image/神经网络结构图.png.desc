图片文件：神经网络结构图.png

图片描述：一张展示深度神经网络结构的示意图，包含输入层、多个隐藏层和输出层。

具体内容：
- 输入层：3个输入节点（x1、x2、x3），代表三个输入特征
- 隐藏层1：4个神经元节点，标注为"隐藏层1（ReLU激活）"
- 隐藏层2：4个神经元节点，标注为"隐藏层2（ReLU激活）"
- 隐藏层3：3个神经元节点，标注为"隐藏层3（ReLU激活）"
- 输出层：2个输出节点（y1、y2），代表二分类结果
- 各层之间用箭头连接，展示数据流向
- 每条连接线上标注有权重值 wij

关键文字区域：
- 标题"深度神经网络结构示意图"
- 层名称标签：输入层、隐藏层1、隐藏层2、隐藏层3、输出层
- 激活函数标签：ReLU、Softmax
- 节点标识：x1、x2、x3、y1、y2

预期OCR结果：应识别出各层名称、节点标识、激活函数类型、数据流向
